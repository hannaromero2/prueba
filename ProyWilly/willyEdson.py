#from Stack import Stack
import sys





pregunta = input("Ingrese su expresion \n") ## ingresa la pregunta 1*
respuesta(pregunta) ##llama a la funcion respuesta que verifica que no sea un comando 2*

def expresion(exp):
    # crear la funcion


def respuesta(resp): ## entra la pregunta a la funcion de respuesta y checa que es ( expresion o comando de salir)
    if(resp == 'Salir' or resp or 'SALIR' or resp == 'salir'):  ## si introduces salir en el comand , te saca 3*
        print('hola \n') #prueba de que entra en la funcion (borrar)
        sys.exit()
    else:    ## en caso de no ser fucnion llama a la otra funcion para checar la expresion
    expresion(resp) #llama a la funcion de expresion que realiza todo de verificar los datos y hace el procedimeinto
